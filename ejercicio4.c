#include <stdio.h>

void main(void){

    /*Quiero que me hagas un codigo con el siguiente contenido.
    vamos a trabajar punteros
    crea una variable del tipo que sea
    atribulle un valor a esa variable
    imprimela de la siguiente manera ("Esto es el valor de la variable original + variable")
    crea un puntero que apunte a esa variable
    Imprime el valor de la variable desde el puntero

    imprime la direccion de memoria de ambas de el puntero y de la variable, deberian de ser iguales
    
    Imprime el valor mas 1 de la variable y despues el del puntero

    cambia el valor del puntero y veras que tambien cambia el valor de la variable

    */

}